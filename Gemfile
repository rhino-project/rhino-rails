# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# ostruct is no longer a default gem on Ruby 3.5+/4.x and is required by the
# test suite (spec/feature specs). Pin it explicitly so specs load.
gem "ostruct"
